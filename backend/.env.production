# Production Environment Configuration
# Copy this to .env and update with your production values

# Application Settings
APP_NAME=PathFinder Academic Dashboard
APP_VERSION=1.0.0
DEBUG=False
HOST=0.0.0.0
PORT=9000

# CORS Settings - UPDATE WITH YOUR EC2 IP/DOMAIN
ALLOWED_ORIGINS=http://YOUR_EC2_PUBLIC_IP:5173,http://YOUR_DOMAIN.com,https://YOUR_DOMAIN.com

# Cassandra Database Configuration (External - No changes needed)
CASSANDRA_HOST=sunway.hep88.com
CASSANDRA_PORT=9042
CASSANDRA_KEYSPACE=subjectplanning
CASSANDRA_DATACENTER=datacenter1
CASSANDRA_USERNAME=planusertest
CASSANDRA_PASSWORD=Ic7cU8K965Zqx

# JWT Configuration - GENERATE NEW SECRET FOR PRODUCTION!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=GENERATE_NEW_SECRET_KEY_HERE_DO_NOT_USE_DEFAULT
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Logging
LOG_LEVEL=INFO
